# language: android
# sudo: required
# jdk: oraclejdk8
# script:
#   - sudo su
# android:
#   components:
#     - build-tools-27.0.3
#     - extra-google-google_play_services
# before_script:
#   - android list sdk --all
#   - yes | sdkmanager "platforms;android-27"
#   - echo "y" | android update sdk --no-ui --all -t 72
#   - android list targets
#   - echo no | android create avd --force -n test -t android-27 --abi "google_apis/x86"
#   #- yes | sdkmanager create avd -f -n test -k "system-images;android-27;google_apis;x86"
#   - emulator -avd test -no-audio -no-window &
#   - android-wait-for-emulator
#   - adb shell input keyevent 82 &
# script:
#   - ./gradlew verifyMode screenshotTests